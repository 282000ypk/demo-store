[build]
  publish = "static"
  functions = "netlify/functions"

[build.environment]
  PYTHON_VERSION = "3.10"

# Forward all requests to the Flask serverless function
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"
  status = 200
